/**
 * Portal HTTP client — re-exports shared primitives from @hermes/ui.
 * Any portal-specific helpers (e.g., portal-only endpoints) go here.
 */
export { fetchJSON, getSessionToken } from "@hermes/ui";
