/**
 * Ops HTTP client — re-exports shared primitives from @hermes/ui.
 * Ops-only helpers (gateway-specific, etc.) can be added here.
 */
export { fetchJSON, getSessionToken, gatewayFetch } from "@hermes/ui";
